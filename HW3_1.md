# HW3-1: Naive DQN for Static Mode 程式碼理解與探討報告

## 1. 基礎環境設定：Gridworld 靜態模式 (Static Mode)
根據教材與環境設定，`mode='static'` 代表這是一個 4x4 的網格世界，其中所有的物件都是固定位置的：
- **Player (P)**: 玩家，固定起點於 (0,3)
- **Goal (+)**: 目標，固定於 (0,0)，抵達可獲得 Reward +10
- **Pit (-)**: 陷阱，固定於 (0,1)，掉入會獲得 Reward -10
- **Wall (W)**: 牆壁，固定於 (1,1)，玩家無法穿過

因為所有位置皆固定不變，這個模式非常適合用來測試演算法的基本邏輯與神經網路收斂的正確性。

---

## 2. Naive DQN 實作解析 (Basic DQN Implementation)

### 網路結構與設計
在這個基礎環境中，輸入狀態是一個 $4 \times 4 = 16$ 轉換而來 $64$ 維的張量（代表網格特徵編碼狀態）。
使用的神經網路是一個簡單的多層感知機 (MLP)：
- **輸入層**: 64 個節點
- **隱藏層 1**: 150 個節點 (ReLU 激活)
- **隱藏層 2**: 100 個節點 (ReLU 激活)
- **輸出層**: 4 個節點（分別代表上、下、左、右 4 個動作的 Q-value）

### 訓練流程與缺點
在 Naive DQN 的實作中，Agent 使用 $\epsilon$-greedy 策略來探索環境，並且每一時間步 (step) 都即時進行一次 Backpropagation 學習：
1. Agent 觀察當前狀態 $S_t$，透過模型預測 Q 值，並以機率 $\epsilon$ 隨機選擇動作或選擇最大 Q 值的動作 $A_t$。
2. 執行動作後，得到 Reward $R_{t+1}$ 以及下一個狀態 $S_{t+1}$。
3. 直接計算目標值 (Target) $Y = R_{t+1} + \gamma \max Q(S_{t+1})$。
4. 計算預測 Q 值與目標值 Y 之間的損失 (MSE Loss)，並即刻更新神經網路權重。

**Naive DQN 的問題**：
- **樣本高度相關 (High Correlation)**：Agent 每走一步就學一步，導致相鄰訓練數據之間的關聯性極高，神經網路容易對近期的軌跡發生 Overfitting（也就是只記得最近走的幾步，忘了全局策略）。
- **目標不穩定 (Moving Target)**：用同一個網路去預測現有 Q 值和目標 Q 值，這使得目標值不斷浮動，訓練過程極不穩定。

---

## 3. 經驗回放池 (Experience Replay Buffer) 實作解析

為了改善 Naive DQN 的缺點，我們引入了 **Experience Replay Buffer (經驗回放池)** 的機制。

### 核心概念
Agent 在與環境互動時，不馬上把收集到的數據拿去訓練，而是把每一步的經驗打包成一個元組 `(State, Action, Reward, Next_State, Done)`，然後存入一個固定大小（例如 `maxlen=1000`）的雙向佇列 (Deque) 中。這個資料結構就是 Replay Buffer。

### 訓練機制的改善
- **隨機抽樣 (Random Sampling)**：當 Replay Buffer 內的資料量超過一個 Mini-batch 大小（例如 200）時，Agent 會從 Buffer 中「隨機」抽出 200 筆不連續的經驗進行神經網路的批次更新。
- **打破相關性 (Break Correlation)**：隨機抽樣打破了時間序列上的數據相關性，讓模型不會只記住剛走過的路徑，使得梯度更新方向更具全局代表性。
- **提高樣本利用率 (Data Efficiency)**：同一個經驗會保留在 Buffer 中一段時間，有機會被反覆抽樣學習，極大地提高了資料的使用率。

### 程式碼特徵
在 Experience Replay 的實作特徵如下：
```python
# 初始化經驗回放池
replay = deque(maxlen=mem_size)

# 將經驗存入 replay buffer
exp = (state1, action_, reward, state2, done)
replay.append(exp)

# 當 buffer 收集到足夠的經驗後，進行隨機抽樣與批次訓練
if len(replay) > batch_size:
    minibatch = random.sample(replay, batch_size)
    # 將抽樣的經驗拆解並轉換為 Tensor，以此計算 Loss 進行批次更新
    ...
```

---

## 總結
1. **Static Mode** 是測試 DQN 演算法基礎邏輯最好的起點，由於起點和終點都不變，Naive DQN 在靜態模式下也有機會順利收斂。
2. **Experience Replay Buffer** 是讓 DQN 表現大幅提升的核心技術。它將傳統的「序列在線學習」轉化為「隨機離線批次學習」，是後續應對 `player` 模式與 `random` 模式等動態環境的必備機制。
