# HW3-2: Enhanced DQN Variants for Player Mode 報告

在 `player` 模式下，目標、牆壁和陷阱位置固定，但「玩家」的起始位置會隨機生成。這比 `static` 模式的狀態空間大得多，需要演算法具備更好的泛化與決策能力。為此，我們實作並比較兩種 DQN 的改進變體：**Double DQN** 與 **Dueling DQN**。

## 1. Double DQN

### 1.1 基本原理與改進邏輯
在 Basic DQN 中，計算 Target Q-value 使用的是：
$$Y = R + \gamma \max_{a} Q_{target}(S', a)$$
由於它總是選取下個狀態中最大的 Q 值作為目標，這會導致 **高估偏差 (Overestimation Bias)**。因為如果某些次優動作的 Q 值因為噪聲或初始化原因被高估，Agent 就會傾向選取它們，導致不穩定。

**Double DQN 的解法**：
將「動作選擇 (Action Selection)」與「動作評估 (Action Evaluation)」解耦：
- 使用 **Main Network** ($Q_{main}$) 找出下一個狀態中最好的動作。
- 使用 **Target Network** ($Q_{target}$) 來評估該動作的真實 Q 值。
$$Y = R + \gamma Q_{target}(S', \arg\max_{a} Q_{main}(S', a))$$
這樣能大幅減少過度樂觀的高估，使 Q 值的估計更貼近實際期望回報。

### 1.2 程式碼實作重點
在計算 Loss 前，我們不直接取 Target Network 的 max，而是：
```python
with torch.no_grad():
    # 1. Main Network 負責「選擇」動作
    Q1_next = model(state2_batch)
    action_next = torch.argmax(Q1_next, dim=1)
    
    # 2. Target Network 負責「評估」該動作的 Q 值
    Q2_next = model2(state2_batch)
    Q2_next_action_values = Q2_next.gather(dim=1, index=action_next.unsqueeze(dim=1)).squeeze()
    
    # 計算目標 Y
    Y = reward_batch + gamma * ((1-done_batch) * Q2_next_action_values)
```

---

## 2. Dueling DQN

### 2.1 基本原理與改進邏輯
在基本的 DQN 網路架構中，輸出層直接給出每個動作的 Q 值。但在許多狀態下，選擇哪個動作其實根本沒差（例如遠離目標且周圍沒有障礙物時），此時只需要知道該「狀態」本身的價值就好。

**Dueling DQN 的解法**：
它將神經網路的末端拆分成兩條路徑（Streams）：
1. **Value Stream ($V(S)$)**：評估該「狀態」本身有多好。
2. **Advantage Stream ($A(S, a)$)**：評估在該狀態下，選擇特定「動作」相對於其他動作有多大優勢。

最後在輸出層組合：
$$Q(S, a) = V(S) + \left( A(S, a) - \frac{1}{|\mathcal{A}|} \sum_{a'} A(S, a') \right)$$
*(減去平均值是為了增加可辨識性與穩定性)*。

這種架構的優勢在於，神經網路可以更頻繁且獨立地學習到各狀態的價值 $V(S)$，即使沒有執行過所有動作，依然能有效評估該狀態的優劣。因此在像 `player` 模式這類初始狀態變化多的環境中，學習效率能大幅提升。

### 2.2 程式碼實作重點
自定義神經網路結構，將最後一層拆分：
```python
class DuelingDQN(torch.nn.Module):
    def __init__(self):
        super(DuelingDQN, self).__init__()
        self.fc1 = torch.nn.Linear(64, 150)
        self.fc2 = torch.nn.Linear(150, 100)
        # 拆分成兩條路徑
        self.value_stream = torch.nn.Linear(100, 1)
        self.advantage_stream = torch.nn.Linear(100, 4)

    def forward(self, x):
        x = torch.nn.functional.relu(self.fc1(x))
        x = torch.nn.functional.relu(self.fc2(x))
        
        value = self.value_stream(x)
        advantage = self.advantage_stream(x)
        
        # 組合 Q 值：Q = V + (A - mean(A))
        q_vals = value + (advantage - advantage.mean(dim=1, keepdim=True))
        return q_vals
```

---

## 3. 綜合比較與總結

| 模型變體 | 改善的核心痛點 | 實作差異 | 學習效率與穩定性 |
| :--- | :--- | :--- | :--- |
| **Basic DQN** | (基準) | 直接輸出 Q 值，使用 Max 計算 Target | `player` 模式下容易高估，收斂較慢。 |
| **Double DQN** | Q 值被高估 (Overestimation) | Target 計算方式改變，依賴兩個網路。 | 有效避免盲目樂觀，策略更加穩定。 |
| **Dueling DQN** | 評估冗餘 (無需評估所有動作) | 網路架構改變，拆分出 Value 與 Advantage。 | 在無用動作多的狀態下收斂極快，泛化力更強。 |

這兩種改良皆可直接建立在 Experience Replay 與 Target Network 的基礎之上。在 Gridworld 的 `player` 模式中，這兩種演算法相較於基本 DQN，不僅收斂所需時間更短，測試階段（Test Phase）的勝率也能顯著且穩定地超過。
