# HW3-3: Enhance DQN for Random Mode WITH Training Tips 報告

在隨機模式（Random Mode）中，不僅玩家的起始位置隨機，連目標（Goal）、陷阱（Pit）與牆壁（Wall）的位置也全都是隨機配置。為了應對如此複雜且充滿不確定性的環境，我們將原本純手工撰寫的 PyTorch DQN 訓練迴圈，重構成 **PyTorch Lightning** 框架的寫法，藉此引入更高階的訓練管理與多種能穩定學習的優化技巧（Training Tips）。

## 1. PyTorch Lightning 架構重構
PyTorch Lightning 是一個高階的模型封裝框架，能將複雜的訓練邏輯（如迴圈、裝置管理、優化器步進、Log 紀錄）封裝起來，讓程式碼更具可讀性且更容易擴充。我們建立了 `LitDQN` 類別並繼承自 `pl.LightningModule`，主要的改寫重點如下：

1. **`setup`（預熱階段）**：在訓練開始前，先使用隨機動作塞滿 Replay Buffer (Warm-up)。這是為了解決一開始 Buffer 資料不足，無法抽取足夠的 mini-batch 來訓練的問題。
2. **`training_step`（訓練步驟）**：傳統監督式學習是在這個步驟接收資料，而在強化學習中，我們改在這邊進行環境互動 (Environment Step) 並存入 Buffer，接著馬上從 Buffer 抽樣進行神經網路權重更新並回傳 Loss 給 Trainer。
3. **`configure_optimizers`（優化器與排程設定）**：直接回傳定義好的 Optimizer 與 Learning Rate Scheduler，後續由 Trainer 自動處理參數更新。

---

## 2. 訓練技巧提升 (Training Tips Integration)
針對 Random Mode 狀態變異大、難以收斂的情況，我們實作了 3 個關鍵的優化技巧 (Training Tips) 來穩定 DQN 的表現：

### 💡 Tip 1: 梯度裁剪 (Gradient Clipping)
在強化學習中，目標 Q 值 ($Y$) 經常是浮動的。有時候從 Replay Buffer 抽樣出的某一筆經驗會造成極大的誤差（例如突然掉入陷阱獲得 -10，或者首度發現終點獲得 +10），導致反向傳播時發生**梯度爆炸 (Exploding Gradients)**，一口氣破壞原本已經學習好的策略權重。
- **實作方式**：透過 PyTorch Lightning 的 Trainer 參數，直接設定 `gradient_clip_val=1.0`。
- **效果**：限制梯度的最大範數，強制將每一次的權重更新控制在安全範圍內，大大提升訓練早期的穩定性。

### 💡 Tip 2: 學習率排程 (Learning Rate Scheduling)
一開始我們希望模型有較大的學習步長去探索環境並學習大方向的規則；到了訓練後期，網路逐漸收斂，如果維持過大的學習率，反而會導致權重在最佳解附近不斷震盪而無法穩定。
- **實作方式**：在 `configure_optimizers` 內使用 `torch.optim.lr_scheduler.StepLR(optimizer, step_size=5000, gamma=0.9)`。
- **效果**：設定每經過 5000 個 steps，學習率就自動衰減 10%，幫助模型在後期做微調 (Fine-tuning)，收斂到更精確的 Q 值。

### 💡 Tip 3: Huber Loss (Smooth L1 Loss) 取代 MSE Loss
原本的基礎 DQN 使用的是 `MSELoss` (均方誤差)。當預測 Q 值與目標 Y 差距極大時，MSE 會將誤差平方，產生不成比例的巨大懲罰訊號。
- **實作方式**：在計算 loss 時改用 `nn.SmoothL1Loss()(X, Y.detach())`。
- **效果**：Huber Loss 在誤差較小時呈現二次函數 (L2) 特性，在誤差較大時則呈現線性函數 (L1) 特性。這讓模型對於離群值 (Outliers，如未曾見過的新奇狀態突變) 更具魯棒性，是穩定 DQN 非常常用的技巧。

---

## 3. 執行與結論
我們將互動的環境總步數設定為 50,000 steps。你可以透過執行 `run_hw3_3.py` 來親自驗證這個實作：
```bash
python run_hw3_3.py
```
*(請注意：執行前可能需要確保環境內有安裝 PyTorch Lightning，如未安裝可使用 `pip install pytorch-lightning` 進行安裝)*

藉由 PyTorch Lightning 結構化程式碼，再搭配上 **Gradient Clipping**、**LR Scheduling** 與 **Huber Loss** 這三大輔助，Agent 面對完全隨機的 Gridworld `random` 模式時，能大幅降低前期訓練崩潰的機率，穩定而有效地提升最後 Test Phase 的勝率。
